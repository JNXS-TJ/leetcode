class Solution {
public:
    bool queryString(std::string S, int N){
        while(N>0){
            auto a = std::bitset<32>(N--).to_string();
            if(S.find(a.substr(a.find("1"))) == std::string::npos) return false;
            }
        return true;
    }
};
